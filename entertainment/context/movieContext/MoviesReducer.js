import { FETCH_MOVIES } from '../ActionTypes';

export default (state, action) => {
    switch (action.type) {
        case FETCH_MOVIES:
            return {
                ...state,

            }
    }

}